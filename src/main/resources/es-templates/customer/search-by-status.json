{
  "from": {{from}},
  "size": {{size}},
  "query": {
    "bool": {
      "must": [
        { "term": { "customerId": "{{customerId}}" } },
        { "term": { "status": "{{status}}" } }
      ]
    }
  }
}
